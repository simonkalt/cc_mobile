# Copy this file to `.secrets` and set the value. The `.secrets` file is gitignored.
#
# Generate a strong random key (e.g. `openssl rand -hex 32`) and share it only through
# a secure channel with trusted integrations.
#
SERVICE_AUTH_KEY=
